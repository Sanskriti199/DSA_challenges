class Solution {
public:
    bool canJump(vector<int>& nums) {
        int a = nums.size() - 1;
        int b=nums.size();
        for (int i = b - 2; i >= 0; --i) {
            if (i + nums[i] >= a) {
                a = i;
            }
        }
        return a == 0;
    
    }
};
